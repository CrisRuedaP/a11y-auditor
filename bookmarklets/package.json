{
  "name": "a11y-auditor",
  "version": "1.0.0",
  "description": "Bookmarklets para auditar la accesibilidad web (WCAG 2.1/2.2) en tiempo real",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "test": "node build.js && node --test test/"
  },
  "license": "MIT",
  "devDependencies": {
    "playwright": "^1.61.1"
  }
}
