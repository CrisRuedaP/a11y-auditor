# SO / editores
.DS_Store
Thumbs.db
*.swp

# Node (solo se usa para bookmarklets/build.js, sin dependencias hoy)
node_modules/
npm-debug.log*

# Configuración local de Claude Code (skills de terceros, no del proyecto)
.agents/
skills-lock.json

# Notas internas de trabajo con el agente
AGENTS.md
